# LaTeX auxiliary files
*.aux
*.log
*.lof
*.lot
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg

# LaTeX build directories
_build/
_texbuild/
build/

# Patches, cleanup scripts e relatórios de revisão crítica (artefatos de iteração,
# NÃO commitar — usar localmente e descartar)
tcc_revisao_critica*.patch
tcc_revisao_critica*.diff
tcc_final_corrigido*.tex
tcc_final_corrigido*.pdf
tcc_final_p?.tex
tcc_final_p??.tex
tcc_final_p?.pdf
tcc_final_p??.pdf
cleanup_p?.sh
cleanup_p??.sh
PROMPT_aplicar_e_validar*.md
VALIDACAO_P?.md
VALIDACAO_P??.md
*.before*
*.orig
*.rej

# Figuras individuais por modelo (opcionais, geradas só com SAVE_INDIVIDUAL_FIGS=True)
graficos/individuais/

# Outputs regeneráveis do pipeline (gerados por 00_pipeline_preprocessamento.py)
# Para regenerar localmente: cd notebooks_treino && python3 00_pipeline_preprocessamento.py
dados/data_treino_v2.csv

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
